# last cursor position
ctrl + o

# set buffer as "Scratch" type (not prompted to save on exit)
set buftype=nofile

# current buffer not listed in :ls
set nobuflisted

# show all mappings
:map

# show all mappings that begin with <leader>
# for example, if leader is \
:map \
